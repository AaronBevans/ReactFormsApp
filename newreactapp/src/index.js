import React from 'react';
import ReactDOM from 'react-dom';
import App from './App.js'//must import the actual app file to be rendered into the DOM.


ReactDOM.render(<App />, document.getElementById("root"));